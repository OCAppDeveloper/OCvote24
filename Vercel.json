{
  "buildCommand": "pnpm install && pnpm build",
  "outputDirectory": ".output",
  "cleanUrls": true,
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/.output/server/index.mjs"
    }
  ]
}
